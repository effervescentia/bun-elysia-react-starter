apiVersion: batch/v1
kind: Job
metadata:
  name: "{{ include "__app__-db.fullname" . }}-migration"
  labels:
    {{- include "__app__-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "1"
spec:
  template:
    metadata:
      labels:
        {{- include "__app__-db.labels" . | nindent 8 }}
    spec:
      containers:
      - name: "{{ .Chart.Name }}-migration"
        image: "{{ .Values.migration.image.repository }}:{{ .Values.migration.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.migration.image.pullPolicy }}
        envFrom:
          - secretRef:
              name: "{{ include "__app__-db.fullname" . }}-migration-secrets"
          - configMapRef:
              name: "{{ include "__app__-db.fullname" . }}-migration-config"
      restartPolicy: Never
