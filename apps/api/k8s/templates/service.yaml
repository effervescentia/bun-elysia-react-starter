apiVersion: v1
kind: Service
metadata:
  name: {{ include "__app__-api.fullname" . }}
  labels:
    {{- include "__app__-api.labels" . | nindent 4 }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/component: server
    {{- include "__app__-api.selectorLabels" . | nindent 4 }}
